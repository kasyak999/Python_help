sudo ip addr add 10.42.0.1/24 dev eth0
sudo ip link set eth0 up

sudo apt install dnsmasq
sudo nano /etc/dnsmasq.conf

черех нетворк менеджер
sudo nmcli con add type ethernet ifname eth0 con-name eth0-share ipv4.method shared

Включи IP forwarding:
sudo sysctl -w net.ipv4.ip_forward=1

что бы постоянно включить
sudo nano /etc/sysctl.conf
net.ipv4.ip_forward=1


и последнее
sudo iptables -t nat -A POSTROUTING -o wlan0 -j MASQUERADE
sudo iptables -A FORWARD -i wlan0 -o eth0 -m state --state RELATED,ESTABLISHED -j ACCEPT
sudo iptables -A FORWARD -i eth0 -o wlan0 -j ACCEPT

sudo apt install iptables-persistent
Во время установки:
Yes → сохранить IPv4 rules
Yes → (если нужно) IPv6 rules
После этого твои текущие правила будут сохранены.




sudo iptables -t nat -A POSTROUTING -o tun0 -j MASQUERADE && \
sudo iptables -A FORWARD -i eth0 -o tun0 -j ACCEPT && \
sudo iptables -A FORWARD -i tun0 -o eth0 -j ACCEPT

Включить впн
sudo openvpn Downloads/rpi.ovpn
sudo iptables -t nat -A POSTROUTING -o tun0 -j MASQUERADE && sudo iptables -A FORWARD -i eth0 -o tun0 -j ACCEPT && sudo iptables -A FORWARD -i tun0 -o eth0 -j ACCEPT


Временое отключение сна ssh
sudo iw dev wlan0 set power_save off
проверка
iw dev wlan0 get power_save